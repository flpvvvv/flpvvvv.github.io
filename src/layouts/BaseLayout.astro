---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  image?: string;
}

const {
  title,
  description = 'flpvvvv is a data scientist publishing long-form field notes on statistics, inference, probability, and machine learning methods.',
  image = '/images/social.jpg',
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const pathname = Astro.url.pathname;
const navItems = [
  { href: '/posts/', label: 'Archive' },
  { href: '/tags/', label: 'Topics' },
  { href: '/about/', label: 'About' },
];
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />

    <!-- Primary Meta Tags -->
    <title>{title} | flpvvvv</title>
    <meta name="title" content={`${title} | flpvvvv`} />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={`${title} | flpvvvv`} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL(image, Astro.site)} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={`${title} | flpvvvv`} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={new URL(image, Astro.site)} />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght,SOFT@9..144,300..700,0..100&family=IBM+Plex+Mono:wght@400;500&family=IBM+Plex+Sans:wght@400;500;600;700&family=Source+Serif+4:opsz,wght@8..60,400;500;600;700&display=swap" rel="stylesheet" />

    <!-- Favicon -->
    <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon.png" />

    <!-- Theme color -->
    <meta name="theme-color" content="#2f6963" />

    <script is:inline>
      const storedTheme = typeof localStorage !== 'undefined' ? localStorage.getItem('theme') : null;
      const systemTheme =
        typeof window !== 'undefined' && window.matchMedia('(prefers-color-scheme: dark)').matches
          ? 'dark'
          : 'light';
      const initialTheme = storedTheme || systemTheme;
      document.documentElement.classList.remove('light', 'dark');
      document.documentElement.classList.add(initialTheme);
    </script>
  </head>
  <body class="min-h-screen flex flex-col">
    <a href="#main-content" class="sr-only focus:not-sr-only focus:fixed focus:z-50 focus:top-2 focus:left-2 focus:rounded focus:bg-white focus:px-4 focus:py-2 focus:text-primary-700 focus:shadow-lg dark:focus:bg-neutral-900 dark:focus:text-primary-300">Skip to content</a>
    <div id="reading-progress" style="width: 0%"></div>

    <header class="sticky top-0 z-40 border-b border-neutral-200/80 bg-neutral-50/88 backdrop-blur-xl dark:border-neutral-800/85 dark:bg-neutral-950/84">
      <nav class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
        <div class="flex flex-col gap-5 py-5 lg:flex-row lg:items-end lg:justify-between">
          <a href="/" class="group max-w-xl">
            <span class="section-kicker block text-[0.68rem]">
              flpvvvv
            </span>
            <span class="mt-2 block font-display text-[2rem] leading-none tracking-[-0.04em] text-neutral-950 transition-colors duration-300 group-hover:text-primary-700 dark:text-neutral-50 dark:group-hover:text-primary-300 sm:text-[2.45rem]">
              Field Notes in Statistics
            </span>
            <span class="mt-2 block max-w-lg text-sm leading-6 text-neutral-600 dark:text-neutral-300">
              Data scientist. Long-form writing on inference, probability, and machine learning.
            </span>
          </a>

          <div class="flex items-center justify-between gap-4 lg:justify-end">
            <div class="flex items-center gap-5 sm:gap-7">
              {navItems.map((item) => {
                const isActive =
                  item.href === '/'
                    ? pathname === item.href
                    : pathname === item.href || pathname.startsWith(`${item.href}/`);

                return (
                  <a href={item.href} class="nav-link" aria-current={isActive ? 'page' : undefined}>
                    {item.label}
                  </a>
                );
              })}
            </div>

            <button
              id="theme-toggle"
              class="theme-toggle"
              aria-label="Toggle dark mode"
            >
              <svg class="hidden h-5 w-5 dark:block" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" clip-rule="evenodd"></path>
              </svg>
              <svg class="block h-5 w-5 dark:hidden" fill="currentColor" viewBox="0 0 20 20">
                <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"></path>
              </svg>
            </button>
          </div>
        </div>
      </nav>
    </header>

    <main id="main-content" class="flex-1 relative z-10">
      <slot />
    </main>

    <footer class="relative z-10 border-t border-neutral-200/80 bg-neutral-50/78 backdrop-blur-sm dark:border-neutral-800/85 dark:bg-neutral-950/74">
      <div class="mx-auto grid max-w-6xl gap-8 px-4 py-12 sm:px-6 lg:grid-cols-[minmax(0,1.4fr)_minmax(0,1fr)_auto] lg:px-8">
        <div>
          <p class="section-kicker mb-3 text-[0.68rem]">flpvvvv</p>
          <p class="max-w-xl font-display text-[1.75rem] leading-none tracking-[-0.035em] text-neutral-950 dark:text-neutral-50">
            An editorial notebook for mathematical thinking.
          </p>
          <p class="mt-3 max-w-xl text-sm leading-6 text-neutral-600 dark:text-neutral-300">
            Built as a static publication with Astro and compile-time MathJax, designed to let dense equations breathe.
          </p>
        </div>

        <div class="grid gap-2 text-sm leading-7 text-neutral-600 dark:text-neutral-300">
          <a href="/posts/" class="hover:text-neutral-950 dark:hover:text-neutral-50">Archive</a>
          <a href="/tags/" class="hover:text-neutral-950 dark:hover:text-neutral-50">Topics</a>
          <a href="/about/" class="hover:text-neutral-950 dark:hover:text-neutral-50">About</a>
        </div>

        <div class="flex flex-col items-start gap-4 lg:items-end">
          <div class="flex items-center gap-4">
            <a href="mailto:flpvvvv@live.com" class="text-neutral-500 hover:text-primary-700 dark:text-neutral-400 dark:hover:text-primary-300" aria-label="Email">
              <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 20 20">
                <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"></path>
                <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"></path>
              </svg>
              <span class="sr-only">Email</span>
            </a>
            <a href="https://github.com/flpvvvv" target="_blank" rel="noopener noreferrer" class="text-neutral-500 hover:text-primary-700 dark:text-neutral-400 dark:hover:text-primary-300" aria-label="GitHub">
              <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24">
                <path fill-rule="evenodd" d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" clip-rule="evenodd"></path>
              </svg>
              <span class="sr-only">GitHub</span>
            </a>
          </div>

          <p class="font-mono text-[0.72rem] uppercase tracking-[0.18em] text-neutral-500 dark:text-neutral-400">
            &copy; {new Date().getFullYear()} flpvvvv
          </p>
        </div>
      </div>
    </footer>

    <script is:inline>
      const themeToggle = document.getElementById('theme-toggle');
      const getThemePreference = () => {
        if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
          return localStorage.getItem('theme');
        }

        return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
      };

      const setTheme = (theme) => {
        document.documentElement.classList.remove('light', 'dark');
        document.documentElement.classList.add(theme);
        localStorage.setItem('theme', theme);
      };

      setTheme(getThemePreference());

      if (themeToggle) {
        themeToggle.addEventListener('click', () => {
          const currentTheme = document.documentElement.classList.contains('dark') ? 'dark' : 'light';
          setTheme(currentTheme === 'dark' ? 'light' : 'dark');
        });
      }

      const progressBar = document.getElementById('reading-progress');
      if (progressBar) {
        const updateProgress = () => {
          const scrollTop = window.scrollY;
          const docHeight = document.documentElement.scrollHeight - window.innerHeight;
          const progress = (scrollTop / docHeight) * 100;
          progressBar.style.width = `${Math.min(progress, 100)}%`;
        };

        window.addEventListener('scroll', updateProgress, { passive: true });
        updateProgress();
      }
    </script>
  </body>
</html>